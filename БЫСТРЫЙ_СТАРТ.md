# 🚀 БЫСТРЫЙ СТАРТ - Nexo Messenger

## 1️⃣ ОДНА КОМАНДА ДЛЯ ВСЕГО!

```bash
npm run go
```

**ВСЁ!** Эта команда делает всё сама:
- ✅ Устанавливает зависимости
- ✅ Собирает проект
- ✅ Настраивает базу данных
- ✅ Запускает сервер и фронтенд

Мессенджер доступен на `http://localhost:5173`

---

## 2️⃣ Render - одна команда!

В **Start Command** на Render просто напиши:

```bash
npm run go
```

Или для продакшена:

```bash
npm install && npm run build && npm run db:setup && npm start
```

---

## 3️⃣ Всё уже настроено!

`.env` файл уже заполнен твоими данными:
- ✅ Telegram боты (10 шт)
- ✅ Telegram каналы (4 шт)
- ✅ Discord вебхуки (2 шт)
- ✅ База данных от Render
- ✅ Все ключи

**Ничего менять не нужно!**

---

## 4️⃣ Команды

| Команда | Описание |
|---------|----------|
| `npm run go` | **ГЛАВНАЯ КОМАНДА** - делает всё! |
| `npm run dev` | Запуск разработки |
| `npm run build` | Сборка |
| `npm run db:setup` | Настройка БД |

---

## 🆘 Проблемы?

### "No resources available"
- Боты уже добавлены в .env
- Каналы уже добавлены в .env
- Просто запусти `npm run go`

### Ошибка Redis
```bash
docker run -d -p 6379:6379 redis:latest
```

Или в .env добавь:
```
REDIS_URL=redis://localhost:6379
```

---

**ВСЁ ПРОСТО!** 🎉

`npm run go` = работает всё!
